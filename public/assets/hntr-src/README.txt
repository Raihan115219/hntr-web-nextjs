HNTR.art — source package (July 23, 2026)

ENTRY POINTS
  HNTR.art Desktop.html      Desktop version (plain HTML/CSS/JS)
  HNTR.art Mobile.dc.html    Mobile version (requires support.js in same folder)

SUPPORT
  support.js                 Runtime for the mobile file
  HNTR Docs.html             Docs page linked from the app

ASSETS
  Images at root, plus reveal/ and uploads/ subfolders.
  Keep the folder structure intact — files are referenced by relative path.

Serve the folder from any static web server (or open the HTML files directly).